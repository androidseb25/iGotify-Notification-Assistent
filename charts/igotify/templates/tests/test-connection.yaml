apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "igotify.fullname" . }}-test-connection"
  labels:
    {{- include "igotify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "igotify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
